# Render service descriptor
services:
  - type: web
    name: instagram-bot
    env: python
    branch: main
    runtime: python-3.11
    buildCommand: python -m pip install --upgrade pip setuptools wheel && pip install -r requirements_deploy.txt
    startCommand: gunicorn wsgi:app
    # optional: create DB tables after install
    healthCheckPath: /
