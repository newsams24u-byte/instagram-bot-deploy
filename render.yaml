# Render service descriptor - Docker deployment (NO PILLOW!)
services:
  - type: web
    name: instagram-bot
    env: docker
    branch: main
    dockerfilePath: ./Dockerfile
    startCommand: gunicorn wsgi:app --workers 2 --bind 0.0.0.0:5000 --timeout 60
    healthCheckPath: /
