# Render service descriptor
services:
  - type: web
    name: instagram-bot
    env: docker
    branch: main
    dockerfilePath: ./Dockerfile
    # optional: create DB tables after install
    healthCheckPath: /
