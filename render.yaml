# Render service descriptor for Docker deployment
services:
  - type: web
    name: instagram-bot
    env: docker
    branch: main
    # Dockerfile is auto-detected in the root directory
    healthCheckPath: /
